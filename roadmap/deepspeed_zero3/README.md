# DeepSpeed Zero-3 + CPU Offload

Status: In progress.

- Added DeepSpeed config templates for ZeRO-3.
- Implemented optional CPU offload logic in `training/deepspeed_config.json`.
- Updated `setup_and_train.sh` to accept `--deepspeed` flag.
