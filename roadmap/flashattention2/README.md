# FlashAttention-2 Integration

Status: In progress.

- Added dependencies to `requirements.txt`.
- Implemented a wrapper module `flashattention2.py` (placeholder).
- Updated training scripts to optionally use FlashAttention-2.
