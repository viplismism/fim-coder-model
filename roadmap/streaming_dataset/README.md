# Streaming Dataset Support

Status: In progress.

- Added a `streaming_dataset/` package with a stub for a PyTorch `IterableDataset`.
- Modified training scripts to optionally use streaming mode via `--streaming` flag.
