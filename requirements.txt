# Dependencies for QLoRA FIM training + inference
# Tested with CUDA 12.1, Python 3.10
# Pin the training stack. TRL 0.17 keeps the current SFTConfig(max_length=...)
# and processing_class API while allowing transformers 4.49, which preserves
# DeepSeek Coder code whitespace in tokenizer round trips.

# Core ML
torch>=2.1.0
transformers==4.49.0
trl==0.17.0
peft>=0.19.0
accelerate>=1.0.0
bitsandbytes>=0.45.0

# Data & utils
datasets>=3.0.0
wandb>=0.16.0
pyyaml>=6.0
rich>=13.0.0
numpy<2

# API Server (evaluation)
fastapi>=0.109.0
uvicorn>=0.27.0
requests>=2.31.0

# HuggingFace Hub (push/pull adapters & model cards)
huggingface_hub>=0.26.0

# Optional: flash-attn (compile takes 15-30min, skip if needed)
# pip install flash-attn --no-build-isolation
