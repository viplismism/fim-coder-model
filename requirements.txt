# Dependencies for QLoRA FIM training + inference
# Tested with CUDA 12.1, Python 3.10
# Minimum versions reflect the current TRL/PEFT API (SFTConfig.max_length,
# processing_class) and are required to load adapters saved with PEFT >= 0.19.

# Core ML
torch>=2.1.0
transformers>=4.49.0,<5.0   # cap below 5.0: transformers 5.x mangles the deepseek tokenizer decode (strips whitespace)
trl>=0.18.0
peft>=0.19.0
accelerate>=1.0.0
bitsandbytes>=0.45.0

# Data & utils
datasets>=3.0.0
wandb>=0.16.0
pyyaml>=6.0
rich>=13.0.0
numpy<2

# API Server (evaluation)
fastapi>=0.109.0
uvicorn>=0.27.0
requests>=2.31.0

# HuggingFace Hub (push/pull adapters & model cards)
huggingface_hub>=0.26.0

# Optional: flash-attn (compile takes 15-30min, skip if needed)
# pip install flash-attn --no-build-isolation
